# Supabase — Project Settings → API.
# Copy to .env.local (gitignored) and fill in. The anon key is safe in client
# code as long as RLS is in place; see ../schema.sql and ../SUPABASE.md.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
