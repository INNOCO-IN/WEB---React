export default function Home() {
  return (
    <main>
      <h1>IN — app shell</h1>
      <p>
        Empty React shell. The live site is the static build in <code>site/</code>;
        nothing has been ported into here yet.
      </p>
    </main>
  )
}
