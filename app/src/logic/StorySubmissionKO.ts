import useLogic from './StorySubmissionEN';

/**
 * Story-Submission.KO — the same progressive reveal as the English page.
 *
 * The state is one boolean about whether the visitor has started typing, which
 * is not a thing that differs by language.
 */
export default useLogic;
