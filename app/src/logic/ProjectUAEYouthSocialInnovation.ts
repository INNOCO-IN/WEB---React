import { useGalleryFor } from '../lib/hooks/useGallery';

/** ProjectUAEYouthSocialInnovation — the photo lightbox, captioned in the language of the route. */
export default function useLogic() {
  return useGalleryFor('ProjectUAEYouthSocialInnovation');
}
