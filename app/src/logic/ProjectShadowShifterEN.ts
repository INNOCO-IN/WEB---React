import { useGallery } from '../lib/hooks/useGallery';

/** ProjectShadowShifterEN — the photo lightbox, shared with the other project pages. */
export default function useLogic() {
  return useGallery('ProjectShadowShifterEN');
}
