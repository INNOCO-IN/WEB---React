import { useGalleryFor } from '../lib/hooks/useGallery';

/** ProjectUNCDocumentary — the photo lightbox, captioned in the language of the route. */
export default function useLogic() {
  return useGalleryFor('ProjectUNCDocumentary');
}
