import useConstellation from '../lib/hooks/useConstellation';

/** Constellation — the sky of stories, in whichever language the route names. */
export default function useLogic() {
  return useConstellation();
}
