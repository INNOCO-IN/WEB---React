import useStoryIndex from '../lib/hooks/useStoryIndex';

/** Story-Index.EN — filter, search and read, over the story collection. */
export default function useLogic() {
  return useStoryIndex('EN');
}
