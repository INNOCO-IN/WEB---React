import { useGallery } from '../lib/hooks/useGallery';

/** ProjectUNCDocumentaryEN — the photo lightbox, shared with the other project pages. */
export default function useLogic() {
  return useGallery('ProjectUNCDocumentaryEN');
}
