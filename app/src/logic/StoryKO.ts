import useLogic from './StoryEN';

/**
 * Story.KO — the same four cards as the English page.
 *
 * Which card the reader is pointing at is not a thing that differs by language.
 */
export default useLogic;
