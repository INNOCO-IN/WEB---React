import { useGalleryFor } from '../lib/hooks/useGallery';

/** ProjectJungleJam — the photo lightbox, captioned in the language of the route. */
export default function useLogic() {
  return useGalleryFor('ProjectJungleJam');
}
